{% extends "_layouts/cp" %}
{% import "_includes/forms" as forms %}

{% set title = "Two-Factor Authentication"|t('app') %}
{% set bodyClass = 'two-factor-authentication-settings' %}

{% set content %}

    <h2>{{ 'Why two-factor authentication?'|t('app') }}</h2>
    <p>
        {{ 'With two-factor authentication you can improve protection of your account, because your phone (or other device) is needed as a second step during login.'|t('app') }}<br />
        {{ 'When enabled, login has an extra required step: First you login as usual, second you need to enter a code generated by your personal device.'|t('app') }}
    </p>

    <hr />

    {% if isUserVerified %}

        <h2 class="success">{{ 'Your account is secured.'|t('app') }}</h2>
        <p>{{ 'Two-factor authentication is enabled for your personal account.'|t('app') }}</p>

        <hr />

        <h2>{{ 'Disable two-factor authentication'|t('app') }}</h2>
        <p>{{ 'To disable two-factor authentication, click the button below.'|t('app') }}</p>

        <form id="turnoff-form" action="{{ actionUrl('two-factor-authentication/settings/turnOff') }}" method="post" accept-charset="UTF-8">
            {{ getCsrfInput() }}

            <input id="submit" class="btn submit icon insecure" type="submit" value="{{- "I don't want two-factor authentication"|t('app') -}}">
        </form>

    {% else %}

        <h2>{{ 'Step 1: Download app'|t('app') }}</h2>
        <p>{{ 'Install a TOTP app like {linkOpen}Google Authenticator{linkClose} to set-up.'|t('app', {
            linkOpen: '<a href="http://support.google.com/a/bin/answer.py?hl=en&answer=1037451" target="_blank">',
            linkClose: '</a>'
            })|raw }}
        </p>

        <hr />

        <h2>{{ 'Step 2: Setup your device with your personal secret'|t('app') }}</h2>
        <p>{{ 'To turn on two-factor authentication for your personal account, please use either the secret code or the QR code.'|t('app') }}</p>

        <table id="groups" class="data fullwidth collapsible">
            <thead>
                <th scope="col">{{ 'Your secret code:'|t('app') }}</th>
                <th scope="col">{{ 'Your QR code:'|t('app') }}</th>
            </thead>
            <tbody>
                <tr>
                    <td width="200">
                        <code>
                            {% for chunk in currentUserSecret %}
                                <span>{{ chunk }}</span>
                            {% endfor %}
                        </code>
                        <p class="instructions">{{ 'Make sure to setup using time-based authentication.'|t('app') }}</p>
                    </td>
                    <td>
                        <img src="{{ currentUserQRCode|raw }}" alt="" />
                    </td>
                </tr>
            </tbody>
        </table>

        <hr />

        <h2>{{ 'Step 3: Verify the code from your device'|t('app') }}</h2>
        <p>{{ 'Please enter the code from your device, do note it changes every few seconds.'|t('app') }}</p>

        <form id="verify-form" action="{{ actionUrl('two-factor-authentication/settings/turnOn') }}" method="post" accept-charset="UTF-8">
            {{ getCsrfInput() }}

            {{ forms.textField({ id: "authenticationCode", name: "code", placeholder: "Authentication Code"|t('app') }) }}

            <div class="buttons">
                <input id="submit" class="btn submit disabled icon secure" type="submit" value="{{ "Verify"|t('app') }}">
                <div id="spinner" class="spinner hidden"></div>
            </div>
        </form>

    {% endif %}
{% endset %}
